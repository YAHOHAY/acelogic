services:
  # --- 你的 Python 核心 API 服务 ---
  api:
    build: .
    container_name: acelogic_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # 将本地代码实时映射到容器
    # 覆盖 Dockerfile 的 CMD，强制开启热更新！
    command: uvicorn ace_logic.main:app --host 0.0.0.0 --port 8000 --reload

    env_file:
      - .env
    environment:
      # 让 Python 知道自己在 Docker 里，去连叫 db 的容器
      - DATABASE_URL=postgresql+asyncpg://postgres:password@db:5432/acelogic_db
    depends_on:
      db:
        condition: service_healthy # 🚀 大厂绝招：绝对等待 DB 就绪才启动 API！

  # --- 你的 PostgreSQL 数据库服务 ---
  db:
    image: postgres:15-alpine
    container_name: acelogic_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: acelogic_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # 🚀 大厂绝招：心跳检测探针
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d acelogic_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: